{
  "name": "thinker-mcp",
  "version": "0.2.0",
  "description": "MCP server exposing a `think` tool — pure reasoning with no side effects, modeled after Codebuff's thinker agent. Lets a tool-using agent (e.g. Hermes) delegate hard reasoning to a stronger model via OpenRouter.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "thinker-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
